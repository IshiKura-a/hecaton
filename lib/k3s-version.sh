# Pinned k3s version. Used by both server and agent install scripts; they
# MUST match. Bump deliberately.
#
# Find current channels / versions:
#   https://update.k3s.io/v1-release/channels
#
# Format: vX.Y.Z+k3sN

K3S_VERSION="v1.35.5+k3s1"
